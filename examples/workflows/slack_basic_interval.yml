workflow:
  id: slack-basic-demo
  description: Send a slack message every interval
  triggers:
    - type: interval
      value: 15
  actions:
    - name: trigger-slack
      provider:
        type: aks
        config: " {{ providers.aks }} "
        with:
          command_type: "get_pods"
