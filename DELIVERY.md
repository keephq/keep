# Squadcast Provider for Keep

This package adds a Squadcast provider to Keep, allowing you to trigger, list, acknowledge, and resolve incidents via Squadcast's API.

## Setup

1. Install dependencies:

    pip install -r requirements.txt

2. Run tests to verify:

    make verify

## Usage

Import and use `SquadcastProvider` in your Keep workflows. Configure with your Squadcast API key.
